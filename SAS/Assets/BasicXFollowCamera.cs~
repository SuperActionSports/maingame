using UnityEngine;
using System.Collections;

public class BasicXFollowCamera : MonoBehaviour {

	public GameObject followee;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		transform.position = new Vector3(followee.transform.position.x,transform.position.y,transform.position.z);
	}
}
