using UnityEngine;
using System.Collections;

public class StickeggController : MonoBehaviour {

	// Use this for initialization
	void Start () {
		// string num = PlayerPrefs.GetInt ("numOfPlayers") ;  // get number of players
		// .GetFloat (player#_color(R/G/B))  for player color if wanted
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
